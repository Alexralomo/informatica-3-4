#include <stdio.h>

int main(void)

{
 int number;  // esto no es exacto lo del final por eso esta mal con esta corexion se resuelve

   int test_value; //le falto diagonal y por eso esta mal el programa
   int example1; //no de ve empesar con un numero puede terminar pero no iniciar
   float int_Float; // se le tiene que poner el nomvre a la variable
   printf("Variables created successfully.\n");
}
